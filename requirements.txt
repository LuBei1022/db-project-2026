# RAG 微服务依赖
Flask>=3.0
Werkzeug>=3.0
python-dotenv>=1.0

# 大模型（DeepSeek / Qwen 都是 OpenAI 兼容接口）
openai>=1.0

# 数据库（SQL Server）
pymssql>=2.2

# 向量检索 + 嵌入
langchain>=0.2
langchain-community>=0.2
langchain-core>=0.2
chromadb>=0.4
sentence-transformers>=2.2

# PDF 解析 / OCR（回填全文用，没有 PDF 时也不影响问答）
pdfplumber>=0.11
pytesseract>=0.3
wordninja>=2.0
